1.Comming to the astah7.2-crack to crack the tool 

2.Open the tool
astah-pro

3.How to transform c++ code to the uml gui xml 
    3.0: Put the cplusreverse-1.1.0.jar to the /usr/lib/astah_professional path
        sudo cp -rf cplusreverse-1.1.0.jar /usr/lib/astah_professional/plugins
	3.1.1.Get copy of the repository
	git clone https://github.com/doxygen/doxygen.git
	or another methord
	tar -zxpf doxygenSetup.tar.gz
	cd doxygen
	mkdir build
	cd build
	sudo apt-get install flex
	sudo apt-get install bison
	cmake -G "Unix Makefiles" ..
	make
	3.1.3Install
	sudo make install
	3.1.4
	sudo apt-get install doxygen-gui
	3.1.5
	sudo apt-get install graphviz 
	3.1.6 open the doxygen
	doxywizard
	It is Over
	3.2 Backup mathord ,may not be useful.
	sudo apt-get install libclang-dev
	sudo apt-get install doxygen
	sudo apt-get install doxygen-gui
	sudo apt-get install graphviz       
	doxywizard 
